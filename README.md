This very site — a Pokemon-themed developer portfolio powered by Three.js, GSAP, and Anime.js with a fully typed codebase.

This website is in development stage and many of the data is unreal .

  # Pokemon Tech Portfolio Website

  This is a code bundle for Pokemon Tech Portfolio Website. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  